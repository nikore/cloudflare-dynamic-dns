apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudflare-dynamic-dns.fullname" . }}-test-connection"
  labels:
    {{- include "cloudflare-dynamic-dns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudflare-dynamic-dns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
